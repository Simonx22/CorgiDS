os:
  - osx

before_install:
 - brew install qt5
 - brew link qt5 --force

script:
 - cd CorgiDS
 - qmake CorgiDS.pro
 - make
 - zip -r -X CorgiDS_macOS.zip CorgiDS.app
 - ls

deploy:
  provider: releases
  api_key: $GITHUB_OAUTH_TOKEN
  file: CorgiDS_macOS.zip
  skip_cleanup: true
  on:
    tags: true
    all_branches: true
